{
    "name": "jazzi",
    "version": "1.8.0",
    "main": "./dist/jazzi.cjs.js",
    "license": "MIT",
    "types": "types/index.d.ts",
    "publishConfig": {
        "access": "public"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Jkierem/jazzi"
    },
    "files": [
        "dist",
        "types"
    ],
    "scripts": {
        "build": "yarn build:ts && yarn build:deno",
        "build:ts": "echo 'Not ready for this'",
        "build:deno": "echo 'Not ready for this'",
        "clean": "rm -rf ./dist && rm -rf ./deno",
        "test": "rm -rf ./coverage && jest --coverage --verbose",
        "test:silent": "rm -rf ./coverage && jest --silent",
        "test:watch": "jest --verbose --watch",
        "prepublish": "yarn clean && yarn build"
    },
    "dependencies": {
        "ramda": "0.27.0"
    },
    "devDependencies": {
        "@types/jest": "^27.0.1",
        "jest": "^27.1.1",
        "ts-jest": "^27.0.5",
        "typescript": "^4.4.2"
    }
}
